1.Cube Number Test... Print out all cubed numbers up to the total value 1000. Meaning that if the cubed number is over 1000 break the loop.
cubes = []
for number in range(1, 11):
    cube = number**3
    cubes.append(cube)

for cube in cubes:
    print(cube)


2.Get first prime numbers up to 100
 for i in range(2,101): 
    for j in range(2,101):
        if i%j == 0:
            break
    if i == j:
        print(i,end=",")



3.Take in a users input for their age, if they are younger than 18 print kids, if they're 18 to 65 print adults, else print seniors
user_age = int(input("how old are you?"))

if user_age < 18:
    print("younger")
elif user_age <= 65:
    print("seniors")
